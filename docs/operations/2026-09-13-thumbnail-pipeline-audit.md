# 썸네일·수집·분류·심사 운영 점검

2026-09-13 08:37–08:49 KST 측정. 운영 DB, 실행 중인 컨테이너 코드·로그, 공개 페이지를 대조했다. 배포 후 결과는 아래에 추가한다.

## 썸네일은 심사 등급이 아니다

공개 제품 6,248개 중 3,132개(50.1%)가 내부에 보관한 OG 대표 이미지를 사용한다. 실제 제품 화면을 브라우저로 자동 촬영하는 파이프라인은 없다. 별도 제품 미디어·미디어 선언은 각각 0건이다.

이미지 없는 3,116개는 수집 원본에도 OG 주소가 없는 2,763개와 주소는 있지만 사본이 없는 353개로 나뉜다. 후자는 전부 같은 원인이라고 단정할 수 없다. 현재 캐시 함수는 비정상 응답·허용되지 않은 타입·5MB 초과·접근 제한·네트워크 실패 시 null을 반환하며 이유를 보관하지 않는다. 정기 재수집도 없다.

현재 배포된 상세 화면은 제품 미디어 → 내부 OG → ‘아직 보관된 제품 화면이 없습니다’ 순서다. 홈의 `ProjectCover`는 이미지가 없으면 장식용 UI 패턴을 사용하므로 실제 제품 화면으로 해석하면 안 된다. 작업 디렉터리의 미커밋 `ProductHero.tsx` 변경은 사용자 작업으로 보존했다.

실제 페이지 점검:

- `/p/vigil`: 내부 OG 이미지 로딩 성공. 화면을 직접 확인했다.
- `/p/2024-2026`: 원본 OG가 없어 빈 상태 안내.
- `/p/know-your-vote`: OG 주소는 있지만 캐시 없음. 원본 주소의 응답이 Vercel SSO로 리다이렉트되어 접근 제한을 확인했다.
- 세 페이지 HTTP200·브라우저 오류0. DB의 내부 OG 참조 중 사본 누락0건.

## 파이프라인 관측

| 단계 | 측정 결과 | 판단 |
|---|---|---|
| 수집 | 최근1시간317건,24시간7,555건 원본 확보. 초기 수집 대기0, GitHub transport 재시도 소진10건 | 실제 처리 중. 실패가 전혀 없는 것은 아님 |
| 규칙 판정 | new0, approved0, needs_review912. 최근1시간 발행40개 | 판정·발행 대기 적체 없음. 사람 확인 후보는 남음 |
| 카테고리 분류 | 연결 서비스 connected=true, sonnet 분류 성공23:33:51UTC. 17개 범주 사용. 과거 분류 보류422개는 현재400발행/22거절 | 현재 분류 실패 대기로 오해하면 안 됨 |
| 1차 AI | 최근1시간 성공40회·invalid_output1회. 마지막 정상 잡 완료23:28:17UTC, 이후 약10분 간격 재시도 | README 저장 실패와 잠금 회수 기준 불일치 발견·수정 |
| 2차 AI | pending417→401표(약4분), 실제 결과 증가.23:38UTC pending137후보, failed21후보, needs_human453후보 | 처리 중이나 모델 timeout/형식 오류 및 사람 확인 적체 남음 |
| 별·근거 갱신 | 최근 별 갱신40개 중38성공/2실패, 근거 스캔에서 실제 관측 추가 | 실행 중. 부분 실패를 성공률100%로 표현하지 않음 |

2차 상태는 모델별 표 행이므로 한 후보가 여러 상태에 포함될 수 있다. `resolved`에는 출처 변경·다른 곳에서 결정되어 닫힌 행이 포함된다. 이를 AI 승인 건수로 세면 안 된다.

카테고리 최신15개 표본에서는 회계→Finance, 구매→Commerce, 서체→Design, 경기 결과→Sports 등 합리적인 분류를 확인했다. 전수 의미 정확도 검증은 아니다. `Streamlit`처럼 원본 프레임워크 제목이 제품명이 된 사례와 `Augenta` 소개 문장이 잘린 사례는 콘텐츠 품질 후속 항목이다.

## 심사 정책의 실제 의미

현재 `reviewMode=observe`, `agentEvidence.enforceEligibility=false`, `displayObservedFacts=false`다. 1차 AI 판단은 기록되지만 발행을 막지 않는다. 2차는 위험 신호·1차 결과·5% 표본 등을 검토하며, 공개 제품을 자동으로 내리지 않는다.

공개6,248개를 가장 최근 성공한 1차 Claude 기록과 연결하면 기록없음5,983 / approve49 / reject52 / needs_review164다. 이는 과거 최신 기록을 연결한 수치이며 현재 원본과 해시가 일치하는 유효 승인 수가 아니다. 따라서 ‘공개되어 있으니 AI 심사 통과’라고 표시하거나 보고할 수 없다. 이 점검에서 심사 정책이나 공개 여부를 임의로 변경하지 않았다.

## 확인된 장애와 수정

1. `spmixx12-creator/sperok`의 공개 README562바이트에 NUL10개가 포함되어 있었다. `readmeText`가 이를 그대로 남겨 `setReadmeSample`의 PostgreSQL text/JSONB 저장이 실패했다.
2. DB 오류 메시지에도 원본 NUL이 포함되어 `jobs.last_error` 기록까지 실패했다. 운영 컨테이너에서 제한된 단일 심사를 실행해 PostgreSQL22021(`invalid byte sequence ... 0x00`)을 직접 확인했다. 처리 중 잠금이 남고 실패 이유도 기록되지 않았다.
3. 실행기의 회수 기준은90초지만, 작업 검색·소유권 검증·관리자 표시에는10분이 남아 있었다. 실제23:29:39잠금 다음 재시도는23:39:44였다.

수정은 README에서 NUL 제거, 오류 기록의 NUL 이스케이프, 공유90초 기준 적용이다. 살아 있는 최근 잠금은 계속 보호한다. 모델의 승인/거절 기준은 변경하지 않았다.

## 검증

- 버그를 재현하는 RED 실행:90초 경과 대기열 회수, NUL 포함 오류 기록, NUL 포함 README 모두 수정 전 실패 확인.
- 전체 통합64파일591개 통과(회수 기준 수정 시점).
- 최종 변경 후 관련 통합3파일24개, 단위2파일6개 통과.
- 최종 TypeScript·변경 파일 ESLint·diffcheck·격리 작업 트리 production build 통과.
- 빌드 실패 이력: 기존 `.next/standalone` 정리 중 ENOTEMPTY, 격리 트리 외부 node_modules 심볼릭 링크 Turbopack 거부. 격리 트리에 의존성을 복제하여 해결했다.
- 자체 최종 리뷰:90초 경계, 살아 있는 작업 중복 실행 방지, 이전 소유권 거절, 실패 후 잠금 해제·재시도 예약, 기존 사용자 변경 제외를 확인했다.

원시 집계는 `.crawl-samples/pipeline-audit-2026-09-12T23-37-04-155Z.json`, `pipeline-audit-2026-09-12T23-40-56-393Z.json`, `pipeline-followup.json`, `thumbnail-live-smoke.json`에 있다. 이미지 증거는 `.crawl-samples/thumbnail-{slug}.png`다. 자격 증명은 포함하지 않는다.

## 운영 반영

코드 릴리스 `2ed85c499fd1e80bbcf7070fa8f06a9d9d68cff1` 운영 반영 완료.

- crawler/publisher/reviewer/maintenance:23:50:28–37UTC deployment.done.
- 웹 M3 `0a_bBgIbVmWSudinbZrhf`:23:52:26UTC done. mini `APrLNXZvWrggFyptVhR3O`:23:52:22UTC done. 두 직접 health 응답이 같은 릴리스, status/db 모두ok.
- 1차 심사 정상 완료23:50:53,23:52:54UTC. 후속4개 결과 기록 성공. 문제 저장소 `spmixx12-creator/sperok`도23:52:54UTC `needs_review` 결과 저장 성공, README367자 저장. 작업 잠금·오류null, 요청/처리버전5848/5848.
- 문제 README를 운영 컨테이너의 동일 로더로 다시 읽어 저장본367자/NUL없음을 확인했다. 해당 저장소 심사는 그 이전에 정규 워커가 이미 처리했다.
- 2차 정상 잡 완료23:52:43UTC 이후 다음 틱 진행. 점검 중 pending417→350표, failed19→12표(23:51:40 집계). 실패·사람 확인 적체가 사라졌다는 뜻은 아니다.
- 배포 후23:53UTC 실제 상세3페이지, 홈4×5행·GitHub계정·설명, 전체보기15행·필터·페이지이동·모바일·집계 기준 모두 통과, 브라우저 오류0. 직접 이미지 화면 확인도 완료.
- 증거: `.crawl-samples/review-recovery-2026-09-12T23-53-33-252Z.json`, `pipeline-deployment-status.jsonl`, `thumbnail-live-smoke.json`, `popular-compact-production-smoke.json`.

최종 재검토에서 이미지 자동 촬영/OG 재시도 부재, 관찰 모드, 2차 사람 확인 대기, 일부 원본 제목·설명 품질을 미해결 항목으로 구분했다. 이 점검은 런타임 복구와 표본 검증이며 모든 제품의 적격성·분류 의미를 전수 보증하지 않는다.
