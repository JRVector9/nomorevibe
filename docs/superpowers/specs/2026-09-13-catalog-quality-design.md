# Catalogue quality, search and daily star changes

The user authorized excluding personal research documents and one-off surveys, displaying daily star changes, showing nine project cards and repairing search. Existing user Hero/design artifact changes stay untouched. The latest request replaces the earlier survey-feature question with search repair.

Search reproduction: plain name queries return matching cards but results begin1431px below the viewport; @GitHub-owner queries return nothing. Global header search will search the public catalogue independently of old tab/category selections, lead directly to a result heading, and match literal name/tagline/description/slug/repository owner+name and maker-confirmed builder. Whitespace-separated terms AND together, fields OR per term, URL/owner handles normalized; SQL wildcards remain literal. Ranking filters share the same predicate. Empty queries retain ordinary browsing. A search result page suppresses unrelated home promotion/pulse/popular sections. Keep the catalogue100-row cap explicit; nine initial cards and nine more per click.

Star collection already runs daily per repository (6686/6743 fresh at13:19UTC). Preserve its bounded5-minute batch scheduler. Add previous successful stars and observation time to products; a successful due refresh atomically shifts current→previous before installing current. Failed responses preserve both. Repository changes clear both. UI shows latest stars and signed change versus the previous observation, with timestamps; absence of a real previous observation stays '비교 수집 중'. Do not manufacture historical changes or force an extra same-day refresh. Two observations suffice for the requested change; a historical chart is outside scope.

Non-product scope is the purpose of the page, not keywords alone. Exclude explicit personal research notes/journals/publication pages and respondent-facing one-off research surveys; retain reusable survey builders, reference managers, research tools, recommendation apps with questionnaires. Add one pure bounded detector shared by crawler judgment and publication even for already-approved candidates. Existing published cleanup uses a frozen reviewed cohort, exact id/slug/url/update snapshot and audit metadata, changes only matching crawler products to reversible banned status, and prevents re-entry. Do not change unrelated AI-review policy or blanket-ban research/survey keywords.

Acceptance: reproduce search, add meaningful regression tests; positive/negative purpose fixtures including real OTT survey, Formbricks/JabRef/supplement app; daily positive/negative/zero/failure/repository-edit tests; nine-card and search viewport/browser checks; inspect frozen removal candidates then apply authorized exclusions; migrate/deploy affected services; verify actual pages and jobs; independently review changes, fix findings, record handoff/report/journal.
